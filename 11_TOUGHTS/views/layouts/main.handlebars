<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="shortcut icon" href="/img/favicon.ico" type="image/x-icon">
    {{!-- BEBAS NEUE FONT --}}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap" rel="stylesheet">
    {{!-- DOSIS FONT --}}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Dosis&display=swap" rel="stylesheet">
    {{!-- BOOTSTRAP ICONS --}}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <title>Toughts</title>
</head>
<body>
    <header class="header-main">
        <a href="/">
            <img src="/img/toughts_logo.png" alt="Toughts">
        </a>
        <nav class="nav-main">
            <ul>
                <li>
                    <a class="bebas-neue-regular" href="/">Pensamentos</a>
                </li>
                {{!-- URL's pós autenticação --}}
                {{#if session.userId}}
                    <li>
                        <a class="bebas-neue-regular" href="/toughts/dashboard">Dashboard</a>
                    </li>
                    <li>
                        <a class="bebas-neue-regular" href="/logout">Sair</a>
                    </li>
                {{else}}
                    <li>
                        <a class="bebas-neue-regular" href="/login">Entrar</a>
                    </li>
                    <li>
                        <a class="bebas-neue-regular" href="/register">Registrar</a>
                    </li>
                {{/if}}
            </ul>
        </nav>
    </header>
    <main class="main-main dosis">
        {{!-- Mensagens --}}
        {{#if messages.message}}
            <div class="messages">
                {{messages.message}}
            </div>
        {{/if}}
        {{{body}}}
    </main>
    <footer class="footer-main dosis">
        <p>Direitos reservados. <a href="https://github.com/emanuelfontoura" target="_blank" referrerpolicy="strict-origin-when-cross-origin">Emanuel Fontoura</a></p>
    </footer>
</body>
</html>